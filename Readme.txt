INSTALAR Y CREAR ENTORNO => py -m venv ruta nombreEntorno

PERMISOS DE WINDOWS (Opcional) => Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process

ACTIVAR ENTORNO => nombreEntorno/Scripts/activate

INSTALAR DEPENDENCIAS => pip install -r requirements.txt

SI EXISTEN PROBLEMAS CON EL INTERPRETE => En VSC Ctrl+Shift+P => Select interpreter => Buscar python.exe en venv